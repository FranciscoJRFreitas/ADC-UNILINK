// The code you provided is defining an enumeration called `UserProfileVisibility` in the
// `pt.unl.fct.di.apdc.firstwebapp.util` package.
package pt.unl.fct.di.apdc.firstwebapp.util;

public enum UserProfileVisibility {
    PRIVATE,
    PUBLIC
}
