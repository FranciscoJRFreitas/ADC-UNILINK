// The code you provided is defining an enumeration called `UserActivityState` in the
// `pt.unl.fct.di.apdc.firstwebapp.util` package.
package pt.unl.fct.di.apdc.firstwebapp.util;

public enum UserActivityState {
    INACTIVE,
    ACTIVE
}
